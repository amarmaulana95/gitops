apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: backend-development

resources:
  - ../../common
  - secret.yaml

patches:
  - path: patch/deployment.yaml
    target:
      kind: Deployment
      name: dpl
